<%= form_for(@pcregion) do |f| %>
  <% if @pcregion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pcregion.errors.count, "error") %> prohibited this pcregion from being saved:</h2>

      <ul>
      <% @pcregion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :short %>
    <%= f.text_field :short %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
