= semantic_form_for(@document, :multipart => true) do |f|
  = f.semantic_errors
  = f.inputs do
    = f.input :file, :label => 'File to upload (currently limited to 30Mb)'
    = f.input :name
    = f.input :author
    = f.input :language, :include_blank => true
    = f.input :description
    = f.input :tag_list
    = f.input :roles, :as => :check_boxes
  = f.buttons do
    = f.commit_button
