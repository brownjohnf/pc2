.container.page
  .row
    .twocol.menu_col
      = render 'layouts/menu'
    .eightcol.show_page
      %h1= @page.title
      = markdown(@page.content)
    .twocol.last
      = image_tag('', :width => '200px')
