.container
  .row
    .span-5
      = render 'shared/menu'
    .span-15
      .box
        %h4.inline you are here &rang;
        = link_to 'Home', root_path
        - @page.ancestors.each do |a|
          &rang;
          = link_to a.title, a
      .box
        %h1
          = @page.title
          %span
            last updated
            = time_ago_in_words(@page.updated_at)
            ago
        .tags.right= render 'shared/tags', :tags => @page.tag_list
      = markdown(@page.content)
    .span-4.last
      = avatar_for @page, :size => :thumb
      = button_to 'Edit Page', edit_page_path(@page), :method => :get
      .box
        %h2 Case Studies
      .box
        %h3 Tag Cloud
        = cloud_of_tags(Page)
      - if @page.contributions.any?
        .box
          %h3 Authors
          %ul
          - @page.contributions.each do |c|
            %li= link_to c.user.name, c.user
      = render 'shared/social'
