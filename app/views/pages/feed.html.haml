.container.feed_pages
  .row
    .threecol
      = render 'layouts/menu'
    .sixcol
      = will_paginate @pages
      = render :partial => 'shared/feed_item', :collection => @pages
      = will_paginate @pages
    .threecol.last
