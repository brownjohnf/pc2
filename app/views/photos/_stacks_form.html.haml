- stack = @photo.stacks.build
= bootstrap_form_for stack do |f|
  = f.hidden_field(:stackable_id)
  = f.hidden_field(:stackable_type)
  = f.collection_select :library_id, Library.available(@photo, (can?(:manage, Stack) ? nil : current_user)), :id, :name
  = f.actions do
    = f.submit
