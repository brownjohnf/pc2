= bootstrap_form_for @ticket_owner do |f|
  = f.collection_select :from_id, User.all, :id, :name
  = f.collection_select :to_id, User.all, :id, :name
  = f.collection_select :ticket_id, Ticket.all, :id, :subject
  = f.collection_select :ticket_code_id, TicketCode.all, :id, :name
  = f.actions 
