{
  "timeline": {
    "headline":"Peace Corps Senegal: 50 Years in review",
    "type":"default",
    "startDate":"1963",
    "text":"<p>We're pleased and proud to celebrate fifty years of service in Sénégal. We'd like to take this opportunity to look back on the past, and share stories and photos from our Volunteers past and present.</p><p>If you've got a story, photo or video you'd like to share, you can do so by logging in or signing up on our website <a href=\"/login\">here</a>.",
    "asset": {
    "media":"<%= Photo.first.photo.url %>",
    "credit":"<%= Photo.first.attribution %>",
      "caption":"<%= Photo.first.description %>"
    },
    "date": [
    <% @moments.each do |moment| %>
    {
      "startDate": "<%= moment.datapoint.strftime("%Y,%m,%d") %>",
      "headline": "<%= moment.title %>",
      "text": "<%= json_escape(raw("<p>#{moment.summary}</p><p><div style=\"display:inline-block;float:left;\">#{avatar_for_user(moment.user)}</div>Submitted by #{link_to(moment.user.name, moment.user)}<br />#{(moment.user.volunteers.any? ? Carmen.country_name(moment.user.volunteers.first.country) : Carmen.country_name(moment.user.country))}")) %>",
      "asset": {
      "media": "<%= moment.photo ? moment.photo.photo.url : json_escape(raw("<blockquote>#{truncate(strip_tags(moment.content.gsub(/\n/, '')), :length => 600, :separator => ' ')} #{link_to('read the full story', moment)}</blockquote>")) %>",
        "credit": "<%= json_escape(raw((moment.photo ? link_to(moment.photo.user.name, moment.photo.user) : link_to(moment.user.name, moment.user)))) %>",
        "caption": "<%= json_escape(raw(moment.photo ? moment.photo.description : moment.summary)) %>"
      }
    },
    <% end %>
    {
      "startDate": "<%= Time.now.strftime("%Y,%m,%d") %>",
      "headline": "And here we are today!",
      "text": "Thanks for joining us on this look back. Hope you enjoyed it.",
      "asset": {
      "media": "<%= Moment.last.content.to_json %>",
        "credit": "test",
        "caption": "test"
      }
    }
    ]
  }
}

