= form_for(@moment, :html => { :class => 'form-horizontal' }) do |f|
  .form-actions
    = f.submit 'Save the event!', :class => 'btn btn-primary'
  = render 'fields', :f => f
  .form-actions
    = f.submit 'Save the event!', :class => 'btn btn-primary'
