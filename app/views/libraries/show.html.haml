.container
  .row
    .threecol
      = render 'shared/menu'
    .sevencol
      %h1= @library.name
      %h4 Photos
      - @library.stacks.each do |s|
        = render s.stackable
    .twocol.last
      %h4= @library.description
      %p= @library.country.name
      %p This library is managed by
      = render @library.user
