.container
  .row
    .threecol
      = render 'shared/menu'
    .sixcol
      %h1= @title
      = will_paginate
      %ul
        = render @volunteers
      = will_paginate
    .threecol.last
      %p Peace Corps Volunteers work closely alongside host-country nationals to assist with development, and exchange culture. Every Volunteer is assigned to a region and a sector.
