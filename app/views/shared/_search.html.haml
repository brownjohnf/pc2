= form_tag search_path, :method => :get, :class => 'navbar-form pull-right' do
  = text_field_tag :q, nil, :placeholder => "search the site!"
  = select_tag :resource, options_for_select(['all'], 'all'), :class => 'span1'
  = submit_tag 'search', :class => 'btn btn-success'
