- error ||= a
- if error.errors.any?
  #error_explanation
    %h4= #{pluralize(error.errors.count, "error")} prohibited this object from being saved:
    - error.errors.full_messages.each do |msg|
      %p= msg
