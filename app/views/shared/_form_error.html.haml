- error ||= a
- if error.errors.any?
  .alert.alert-error
    %h4 Uh oh. We've got a problem. #{pluralize(error.errors.count, "error")} prohibited this object from being saved:
    %ol
      - error.errors.full_messages.each do |msg|
        %li= msg
