.container
  .row
    .span-5
      = render 'shared/menu'
    .span-15
      %h1= @title
      = will_paginate
      %ul
        = render @users
      = will_paginate
    .span-4.last
      %p Users are people who've signed in to the site. They may not be members of our organization.
