%li.user
  = avatar_for_user user
  %h4= link_to user.name, user
  %p
    = user.country
    %br
    = link_to user.volunteers.first.sector.name, user.volunteers.first.sector if user.volunteers.any? && user.volunteers.first.sector
  .clearfloat
