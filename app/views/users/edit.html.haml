.container
  .row
    .threecol
      = render 'layouts/menu'
    .sevencol
      - @user.volunteers.build if @user.volunteers.empty?
      - @user.blogs.build if @user.blogs.empty?
      = render 'form'
    .twocol.last
