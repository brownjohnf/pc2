<%= form_for(@regiontype) do |f| %>
  <% if @regiontype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@regiontype.errors.count, "error") %> prohibited this regiontype from being saved:</h2>

      <ul>
      <% @regiontype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
